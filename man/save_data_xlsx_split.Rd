% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/excel.R
\name{save_data_xlsx_split}
\alias{save_data_xlsx_split}
\title{Save a list of data.frames to an Excel workbook (one sheet per table)}
\usage{
save_data_xlsx_split(
  data,
  file,
  rc = c(1L, 1L),
  row_names = FALSE,
  sheet_names = NULL,
  data_titles = NULL,
  title_size = 16,
  font_name = getOption("instead.font"),
  font_size = 14,
  border_colour = "#000000",
  widths = 8.43,
  auto_width = TRUE,
  overwrite = FALSE
)
}
\arguments{
\item{data}{A data.frame, or a named/unnamed list of data.frames (or matrix).}

\item{file}{Path to the target \code{.xlsx} workbook.}

\item{rc}{A length-2 integer vector giving the common starting row and column
for every sheet (default \code{c(1L, 1L)}).}

\item{row_names}{Logical; whether to include row names. Default \code{FALSE}.}

\item{sheet_names}{Optional character vector of sheet names. If \code{NULL},
falls back to \code{names(data)} or autogenerated names.}

\item{data_titles}{Optional character vector of table titles, length \code{length(data)}.
Written \strong{above} each table. If \code{NULL}, no titles are written.}

\item{title_size}{Numeric font size for title rows. Default \code{16}.}

\item{font_name, font_size, border_colour, widths}{Styling forwarded to \code{\link[=write_data]{write_data()}}.}

\item{auto_width}{Logical. If \code{TRUE}, compute per-column widths and apply to data columns.
Default \code{TRUE}.}

\item{overwrite}{Logical; if \code{TRUE}, overwrite an existing workbook. Default \code{FALSE}.}
}
\value{
(Invisibly) the path to \code{file}. Side-effects: creates/updates an Excel file.
}
\description{
Write a data frame or a list of data frames to an Excel workbook using
\code{openxlsx}. When a list is supplied, each element is written to its \strong{own
worksheet}. Sheet names are resolved as:
\enumerate{
\item \code{sheet_names} argument (if provided),
\item else \code{names(data)} if available,
\item else \code{"Sheet 1"}, \code{"Sheet 2"}, ...
}
}
\details{
Optionally writes a title row above each table and auto-sizes data columns
(title excluded from width calculation).
}
\examples{
\dontrun{
d1 <- head(iris)
d2 <- head(mtcars)

# Case 1: explicit sheet_names
save_data_xlsx_split(
  data        = list(d1, d2),
  sheet_names = c("iris_data", "mtcars_data"),
  data_titles = c("Iris Head", "Mtcars Head"),
  file        = "sheets_named.xlsx",
  overwrite   = TRUE
)

# Case 2: names(data) used as sheet names
save_data_xlsx_split(
  data       = list("iris_tab" = d1, "mtcars_tab" = d2),
  file       = "sheets_from_names.xlsx",
  overwrite  = TRUE
)

# Case 3: unnamed list -> Sheet 1, Sheet 2
save_data_xlsx_split(
  data       = list(d1, d2),
  file       = "sheets_auto.xlsx",
  overwrite  = TRUE
)
}
}
