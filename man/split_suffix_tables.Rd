% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/list.R
\name{split_suffix_tables}
\alias{split_suffix_tables}
\title{Split a table into suffix tables}
\usage{
split_suffix_tables(x, by, split_by = NULL, min_nrow = 1L)
}
\arguments{
\item{x}{A data.frame or data.table.}

\item{by}{Character vector of column names defining ordered blocks.}

\item{split_by}{Optional character vector of column names used to split
\code{x} before generating suffix tables.}

\item{min_nrow}{Integer; minimum number of rows required to keep a
generated table. Default \code{1L}.}
}
\value{
A list of data.tables.
}
\description{
Splits a table into an ordered list of suffix tables based on \code{by}.
Each suffix table is created by sequentially removing the earliest
block defined by \code{by}.
}
\details{
If \code{split_by} is supplied, the operation is performed separately within
each group and the resulting tables are concatenated.
}
\examples{
x <- data.frame(
  gender = c("M", "F", "M", "F", "M", "F"),
  age_band = factor(
    c("20-29", "20-29", "30-39", "30-39", "40-49", "40-49"),
    levels = c("20-29", "30-39", "40-49"),
    ordered = TRUE
  ),
  value = 1:6
)

# suffix tables by age band
split_suffix_tables(x, by = "age_band")

# suffix tables computed separately by gender
split_suffix_tables(x, by = "age_band", split_by = "gender")

# keep only tables with at least 2 rows
split_suffix_tables(x, by = "age_band", min_nrow = 2L)

}
