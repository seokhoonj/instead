% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/excel.R
\name{save_image_xlsx_split}
\alias{save_image_xlsx_split}
\title{Save image file(s) to Excel (one sheet per image)}
\usage{
save_image_xlsx_split(
  image,
  file,
  rc = c(1L, 1L),
  sheet_names = NULL,
  image_titles = NULL,
  title_size = 16,
  font_name = getOption("instead.font"),
  width = 12,
  height = 6,
  dpi = 300,
  overwrite = FALSE
)
}
\arguments{
\item{image}{Character vector (or list) of file paths.}

\item{file}{Path to target .xlsx.}

\item{rc}{Length-2 integer vector c(row, col) for top-left position. Default c(1L, 1L).}

\item{sheet_names}{Optional character vector of sheet names; length must equal length(image).
If NULL, names(image) (when present) are used; otherwise "Sheet i".}

\item{image_titles}{Optional character vector of titles (one per image); written one row above the image.}

\item{title_size}{Numeric font size for titles. Default 16.}

\item{font_name}{Font family for title text. Default getOption("instead.font").}

\item{width, height, dpi}{Passed to openxlsx::insertImage().}

\item{overwrite}{Overwrite workbook if TRUE.}
}
\value{
(Invisibly) the path to \code{file}. Side-effects: creates/updates an Excel file.
}
\description{
Writes each image to its own worksheet. A common start cell (\code{rc}) is used
for all sheets. Optional \code{sheet_names} and per-sheet \code{image_titles} supported.
}
\examples{
\dontrun{
# Example images (replace with real PNG/JPG paths on your system)
img1 <- "plot1.png"
img2 <- "plot2.jpg"

# 1) Named list/vector of images: names are used as sheet names
save_image_xlsx_split(
  image        = c("First plot" = img1, "Second plot" = img2),
  file         = "images_split.xlsx",
  rc           = c(1L, 1L),
  image_titles = c("Title for first image", "Title for second image"),
  title_size   = 14,
  overwrite    = TRUE
)

# 2) Unnamed vector: sheets are auto-named ("Sheet 1", "Sheet 2", ...)
save_image_xlsx_split(
  image        = c(img1, img2),
  file         = "images_split_auto.xlsx",
  rc           = c(2L, 2L),   # start at B2
  overwrite    = TRUE
)
}

}
