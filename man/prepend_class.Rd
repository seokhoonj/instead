% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{prepend_class}
\alias{prepend_class}
\title{Prepend class(es) without duplication}
\usage{
prepend_class(x, new_class)
}
\arguments{
\item{x}{An R object.}

\item{new_class}{A character vector of class names to prepend.}
}
\value{
The input object \code{x} with its class attribute updated to have
\code{new_class} at the front, followed by its original classes (excluding
any duplicates).
}
\description{
Ensure one or more class names appear at the front of an object's class
vector, without duplicating existing classes. If \code{x} is a data.table,
uses \code{data.table::setattr()} to avoid copies and preserve selfref.
}
\examples{
\donttest{
df <- data.frame(a = 1)

# Prepend a single class
df <- prepend_class(df, "new_class")

# Prepend multiple classes
df <- prepend_class(df, c("new_class1", "new_class2"))
}

}
