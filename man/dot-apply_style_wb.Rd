% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/excel.R
\name{.apply_style_wb}
\alias{.apply_style_wb}
\title{Apply style rules to a written Excel table (internal)}
\usage{
.apply_style_wb(wb, sheet, data, range, style_rules = NULL, row_names = FALSE)
}
\arguments{
\item{wb}{An \code{openxlsx} \code{Workbook}.}

\item{sheet}{Worksheet name.}

\item{data}{The saved table as a data.frame or data.table.}

\item{range}{A list with elements \code{start_cell} and \code{end_cell}, giving the
written table range in the worksheet.}

\item{style_rules}{Optional list of styling rules.}

\item{row_names}{Logical; whether a row-name column is present in the saved table.}
}
\value{
The modified workbook, invisibly.
}
\description{
Applies one or more cell styles to an already written table in an
\code{openxlsx} workbook. Styling is applied by position using the written table
\code{range}, and may be based either on the saved table itself or on a separate
reference table of the same dimensions.
}
\details{
Each rule in \code{style_rules} must be a list containing:
\itemize{
\item \code{cols}: character column names or numeric column indices in the saved table
\item optional \code{rows}: numeric row indices restricting the rule (default: all rows)
\item optional \code{data}: reference data used to evaluate the rule; if omitted,
the saved table itself is used
\item \code{condition}: a function returning a logical vector; may be of the form
\verb{function(x)} or \verb{function(x, data)}
\item \code{style}: a style object created by \code{\link[=create_style]{create_style()}}
}
}
\examples{
\dontrun{
# Example 1: style based on the saved data itself
wb <- openxlsx::createWorkbook()
openxlsx::addWorksheet(wb, "iris")
dt <- head(iris)

wb <- write_data(wb, "iris", dt, rc = c(1, 1))
rng <- list(start_cell = c(1, 1), end_cell = c(nrow(dt) + 1, ncol(dt)))

.apply_style_wb(
  wb = wb,
  sheet = "iris",
  data = dt,
  range = rng,
  style_rules = list(
    list(
      cols = "Sepal.Length",
      condition = function(x) x > 6,
      style = create_style(fg_fill = "#FF9999")
    )
  )
)

# Example 2: style one column based on another column in the same data
dt2 <- data.frame(
  loading = c(1.2, 0.8, NA),
  decision = c("reject", "fail", "fail")
)

.apply_style_wb(
  wb = wb,
  sheet = "iris",
  data = dt2,
  range = list(start_cell = c(1, 1), end_cell = c(nrow(dt2) + 1, ncol(dt2))),
  style_rules = list(
    list(
      cols = "loading",
      condition = function(x, data) data$decision == "reject",
      style = create_style(fg_fill = "#DDEBF7")
    )
  )
)

# Example 3: style saved data using a separate reference table
value_dt <- data.frame(
  plan = c("A", "B"),
  `20-29` = c(1.2, 0.8),
  `30-39` = c(0.7, 1.5)
)

ref_dt <- data.frame(
  plan = c("A", "B"),
  `20-29` = c("reject", "fail"),
  `30-39` = c("fail", "reject")
)

.apply_style_wb(
  wb = wb,
  sheet = "iris",
  data = value_dt,
  range = list(start_cell = c(1, 1), end_cell = c(nrow(value_dt) + 1, ncol(value_dt))),
  style_rules = list(
    list(
      cols = c("20-29", "30-39"),
      data = ref_dt,
      condition = function(x) x == "reject",
      style = create_style(fg_fill = "#DDEBF7")
    )
  )
)
}

}
\keyword{internal}
