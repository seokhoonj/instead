% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/font.R
\name{set_instead_font}
\alias{set_instead_font}
\alias{get_instead_font}
\title{Set or get the default font for instead outputs}
\usage{
set_instead_font(family)

get_instead_font()
}
\arguments{
\item{family}{A character string giving the font family to use, or \code{NULL}.
\itemize{
\item Use \code{NULL} or \code{""} to reset to system default.
\item Must match a font available in \code{\link[systemfonts:system_fonts]{systemfonts::system_fonts()}}.
}}
}
\value{
\itemize{
\item \code{set_instead_font()}: the font family (character scalar or \code{NULL}) that was set.
\item \code{get_instead_font()}: the current font family (character scalar or \code{NULL}).
}
}
\description{
These helpers manage the global font family used by \code{instead}
(e.g., Excel writers, plot savers), stored in the option \code{"instead.font"}.
}
\details{
\itemize{
\item \code{set_instead_font()} updates the option, verifying that the font exists
in \code{\link[systemfonts:system_fonts]{systemfonts::system_fonts()}}. If the font is not found, the previous
option value is restored and an error is thrown.
\item \code{get_instead_font()} retrieves the currently set option.
}
}
\examples{
\dontrun{
# Set Comic Sans MS if available
set_instead_font("Comic Sans MS")

# Reset to system default (two ways)
set_instead_font(NULL)
set_instead_font("")

# Get current font
get_instead_font()
}

}
