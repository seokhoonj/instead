% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/date.R
\name{date_period_boundaries}
\alias{date_period_boundaries}
\alias{get_week_start}
\alias{get_week_end}
\alias{get_month_start}
\alias{get_month_end}
\alias{get_quarter_start}
\alias{get_quarter_end}
\alias{get_half_start}
\alias{get_half_end}
\alias{get_year_start}
\alias{get_year_end}
\title{Get start or end date of week / month / quarter / half-year / year}
\usage{
get_week_start(x, week_start = 1L)

get_week_end(x, week_start = 1L)

get_month_start(x)

get_month_end(x)

get_quarter_start(x)

get_quarter_end(x)

get_half_start(x)

get_half_end(x)

get_year_start(x)

get_year_end(x)
}
\arguments{
\item{x}{A Date (vectorized OK).}

\item{week_start}{Integer 1–7, where 1 = Monday, ..., 7 = Sunday.
Used only by \code{get_week_start()} / \code{get_week_end()}. Default: 1 (ISO).}
}
\value{
A \code{Date} vector of the same length as \code{x}.
}
\description{
Returns the first or last day of a period that contains \code{x}.
}
\examples{
\donttest{
today <- Sys.Date()

get_week_start(today);  get_week_end(today)
get_month_start(today); get_month_end(today)
get_quarter_start(today); get_quarter_end(today)
get_half_start(today);  get_half_end(today)
get_year_start(today);  get_year_end(today)

# week starts on Sunday
get_week_start(today, week_start = 7)
get_week_end(today,   week_start = 7)
}

}
