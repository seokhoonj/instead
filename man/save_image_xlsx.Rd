% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/excel.R
\name{save_image_xlsx}
\alias{save_image_xlsx}
\title{Save image file(s) to a single Excel sheet (stacked vertically)}
\usage{
save_image_xlsx(
  image,
  file,
  sheet = "Images",
  rc = c(1L, 1L),
  width = 12,
  height = 6,
  dpi = 300,
  image_titles = NULL,
  title_size = 14,
  font_name = getOption("instead.font"),
  row_spacer = 2L,
  rows_per_inch = 5L,
  overwrite = FALSE
)
}
\arguments{
\item{image}{Character vector (or list) of file paths.}

\item{file}{Path to target .xlsx.}

\item{sheet}{Sheet name to write into. Default "Images".}

\item{rc}{Length-2 integer vector c(row, col) for the first image.}

\item{width, height, dpi}{Passed to openxlsx::insertImage().}

\item{image_titles}{Optional character vector of titles (one per image); written one row above the image.}

\item{title_size}{Numeric font size for titles. Default 14.}

\item{font_name}{Font family for title text. Default getOption("instead.font").}

\item{row_spacer}{Non-negative integer: blank rows between images. Default 2.}

\item{rows_per_inch}{Approx rows per 1 inch of image height. Default 5.}

\item{overwrite}{Overwrite workbook if TRUE.}
}
\value{
(Invisibly) the path to \code{file}. Side-effects: creates/updates an Excel file.
}
\description{
Inserts one or more image files into the same worksheet, stacking them.
Optional titles per image and row spacing between images.
}
\examples{
\dontrun{
# Example image files (replace with actual PNG/JPG paths)
img1 <- "plot1.png"
img2 <- "plot2.jpg"

# Stack two images vertically into one sheet
save_image_xlsx(
  image        = c("First" = img1, "Second" = img2),
  file         = "images_stacked.xlsx",
  sheet        = "MyImages",
  rc           = c(1L, 1L),   # start at A1
  image_titles = c("First Image Title", "Second Image Title"),
  title_size   = 16,
  width        = 6, height = 4, dpi = 300,
  row_spacer   = 2L,
  overwrite    = TRUE
)

# Single image
save_image_xlsx(
  image     = img1,
  file      = "single_image.xlsx",
  overwrite = TRUE
)
}

}
\seealso{
\code{\link[=save_image_wb]{save_image_wb()}}, \code{\link[openxlsx:insertImage]{openxlsx::insertImage()}}
}
