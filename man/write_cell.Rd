% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/excel.R
\name{write_cell}
\alias{write_cell}
\title{Write a value into a single cell of an Excel worksheet}
\usage{
write_cell(
  wb,
  sheet,
  rc = c(1L, 1L),
  value,
  bold = FALSE,
  italic = FALSE,
  underline = FALSE,
  strikeout = FALSE,
  font_name = getOption("instead.font"),
  font_size = 11,
  font_color = NULL,
  fg_fill = NULL,
  h_align = NULL,
  v_align = NULL,
  ...
)
}
\arguments{
\item{wb}{A \code{Workbook} object created by \code{\link[openxlsx:createWorkbook]{openxlsx::createWorkbook()}} or
loaded with \code{\link[openxlsx:loadWorkbook]{openxlsx::loadWorkbook()}}.}

\item{sheet}{Sheet name or index to write into.}

\item{rc}{A length-2 integer vector \code{c(row, col)} giving the target cell.}

\item{value}{A scalar value to write (character, numeric, logical, etc.).}

\item{bold, italic, underline, strikeout}{Logical; text decorations to apply.}

\item{font_name}{Character; font family. Default: \code{getOption("instead.font")}.}

\item{font_size}{Numeric; font size in points. Default \code{11}.}

\item{font_color}{Character; text color (hex string or named color). Default \code{NULL}.}

\item{fg_fill}{Character; background fill color. Default \code{NULL}.}

\item{h_align, v_align}{Character; horizontal and vertical alignment
(\code{"left"}, \code{"center"}, \code{"right"} / \code{"top"}, \code{"center"}, \code{"bottom"}).
Passed to \code{\link[openxlsx:createStyle]{openxlsx::createStyle()}}.}

\item{...}{Additional arguments forwarded to \code{\link[openxlsx:writeData]{openxlsx::writeData()}}.}
}
\value{
The modified \code{Workbook} (invisibly), allowing for chaining.
}
\description{
A thin wrapper around \code{\link[openxlsx:writeData]{openxlsx::writeData()}} with styling support.
This function writes a scalar value (character, numeric, logical, etc.)
into a specific \verb{(row, col)} position of a worksheet and applies optional
font/border/align styles.
}
\examples{
\dontrun{
library(openxlsx)

wb <- openxlsx::createWorkbook()
addWorksheet(wb, "Sheet1")

# Write "Hello" into cell B2 with bold styling
write_cell(
  wb, sheet = "Sheet1",
  rc    = c(2L, 2L),
  value = "Hello",
  bold  = TRUE
)

saveWorkbook(wb, "example.xlsx", overwrite = TRUE)
}

}
\seealso{
\code{\link[openxlsx:writeData]{openxlsx::writeData()}}, \code{\link[openxlsx:createStyle]{openxlsx::createStyle()}}
}
