% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/image.R
\name{render_table_html}
\alias{render_table_html}
\title{Render a table as an HTML kable object}
\usage{
render_table_html(
  table,
  caption = NULL,
  footnote = NULL,
  digits = 2,
  full_width = FALSE,
  html_font = getOption("instead.font")
)
}
\arguments{
\item{table}{A data.frame, matrix, tibble, or table object to format.}

\item{caption}{A character string specifying the table caption.}

\item{footnote}{A character vector of footnotes. Each entry will be displayed
using a symbol (e.g., *, †, ‡). Maximum of 20 entries is recommended.}

\item{digits}{Number of digits used for numeric columns. Can be a single
value or a vector matching the number of columns.}

\item{full_width}{Logical; whether to make the table span the full width of
the container. Defaults to \code{FALSE} for more compact display.}

\item{html_font}{CSS font family to apply to the HTML table.}
}
\value{
A \code{kableExtra} HTML table object (not converted to a string).
Typically consumed by \code{view_html()} or \code{render_table_shiny()}.
}
\description{
Create an HTML table using \code{kableExtra}, suitable for viewing in the
RStudio Viewer, web browser, or embedding inside Shiny UI via a higher-level
wrapper. This function returns a kable object \strong{without converting it}
to a character string.
}
\examples{
\dontrun{
# 2x2 Table with totals
set.seed(1)
smoker  <- sample(c("Yes","No"),  100, TRUE)
disease <- sample(c("Yes","No"),  100, TRUE)
tab <- table(Smoker = smoker, Disease = disease)
tab_tot <- addmargins(tab, margin = c(1, 2))
render_table_html(tab_tot, caption = "2x2 Table with Totals")
}

}
