% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{set_ix}
\alias{set_ix}
\title{In-place update by condition or index (wrapper around \code{\link[data.table:assign]{data.table::set()}})}
\usage{
set_ix(dt, i, j, value)
}
\arguments{
\item{dt}{A \code{data.table}, modified by reference.}

\item{i}{Integer or logical vector specifying rows to modify.}

\item{j}{Single column name (character) or column index.}

\item{value}{Replacement vector or evaluated expression.}
}
\value{
Invisibly returns \code{dt} (modified by reference).
}
\description{
Drop-in wrapper for \code{\link[data.table:assign]{data.table::set()}} that allows both numeric and logical
indexing in \code{i}, and automatically subsets \code{value[i]} if \code{length(value) == nrow(dt)}.
}
\examples{
\dontrun{
dt <- data.table::as.data.table(iris)

# Logical indexing (automatic which())
set_ix(dt, dt$Species == "setosa", "Sepal.Length", dt$Sepal.Length * 10)

# Numeric indexing
set_ix(dt, 51:100, "Petal.Width", dt$Petal.Width + 0.5)
}

}
