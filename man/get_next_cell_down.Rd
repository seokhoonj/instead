% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/excel.R
\name{get_next_cell_down}
\alias{get_next_cell_down}
\title{Get the next cell below the last written table}
\usage{
get_next_cell_down(wb, row_spacer = 2L)
}
\arguments{
\item{wb}{An openxlsx \code{Workbook} object that has been written using
\code{\link[=save_data_wb]{save_data_wb()}}.}

\item{row_spacer}{Integer (>= 0). Number of blank rows to leave between the
previous table and the next table. Default \code{2L}.}
}
\value{
A numeric vector \code{c(row, col)} representing the next cell position.
}
\description{
Computes the next top-left cell position \strong{below} the most recently written
table recorded by \code{\link[=save_data_wb]{save_data_wb()}}. The function reads layout metadata stored
in \code{attr(wb, "instead.last_range")}.
}
\details{
The function uses the \code{end_cell} recorded in
\code{attr(wb, "instead.last_range")} and returns:

\code{c(end_row + 1 + row_spacer, start_col)}

This is typically used to stack tables vertically.
}
\seealso{
\code{\link[=save_data_wb]{save_data_wb()}}, \code{\link[=get_next_cell_right]{get_next_cell_right()}}
}
