% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{numify_i64}
\alias{numify_i64}
\title{Convert all integer64 columns to numeric}
\usage{
numify_i64(df)
}
\arguments{
\item{df}{A data.frame, tibble, or data.table.
Columns with class \code{"integer64"} will be converted to numeric.}
}
\value{
\itemize{
\item For data.table input: the same object (modified in place, invisibly).
\item For data.frame/tibble input: a new object of the same class, returned
with integer64 columns converted to numeric.
}
}
\description{
Detects columns of type \code{integer64} (from the \strong{bit64} package)
and converts them to \code{numeric}.
If the input is a \code{data.table}, the conversion is performed \strong{in place}
(by reference, no copy).
For data.frame or tibble inputs, the function returns a modified copy
restored to the original class.
}
\examples{
\dontrun{
library(data.table)
library(bit64)

# data.table example (in-place)
dt <- data.table(a = as.integer64(1:3), b = letters[1:3])
str(dt)
set_i64_to_num(dt)
str(dt) # 'a' converted to numeric

# data.frame example (copy returned)
df <- data.frame(a = as.integer64(4:6), b = letters[4:6])
df2 <- set_i64_to_num(df)
str(df2)
}

}
