% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/excel.R
\name{save_image_wb}
\alias{save_image_wb}
\title{Write image file(s) to a single worksheet in an existing Workbook (stacked)}
\usage{
save_image_wb(
  image,
  wb,
  sheet = "Images",
  rc = c(1L, 1L),
  width = 12,
  height = 6,
  dpi = 300,
  image_titles = NULL,
  title_size = 14,
  font_name = getOption("instead.font"),
  row_spacer = 2L,
  rows_per_inch = 5L
)
}
\arguments{
\item{image}{Character vector (or list) of file paths.}

\item{wb}{An existing \code{openxlsx::Workbook}.}

\item{sheet}{Worksheet name (created if missing). Default \code{"Images"}.}

\item{rc}{Length-2 integer \code{c(row, col)} for the first image’s top-left cell.
Default \code{c(1L, 1L)}.}

\item{width, height, dpi}{Passed to \code{openxlsx::insertImage()}. Defaults:
\code{width = 12}, \code{height = 6}, \code{dpi = 300}.}

\item{image_titles}{Optional character vector of titles (length = \code{length(image)}),
written \strong{above} each image. If \code{NULL}, uses \code{names(image)} when available.}

\item{title_size}{Numeric font size for titles. Default \code{14}.}

\item{font_name}{Font family for title text. Default \code{getOption("instead.font")}.}

\item{row_spacer, rows_per_inch}{Integers controlling vertical spacing in stack mode.
\code{rows_per_inch} is the approximate number of worksheet rows consumed by \strong{one inch}
of image height. Defaults \code{row_spacer = 2L}, \code{rows_per_inch = 5L}.}
}
\value{
The modified \code{Workbook} (invisibly).
}
\description{
Inserts one or more image files (e.g., PNG/JPG) into the same worksheet of
an existing \code{openxlsx} \code{Workbook}, stacked vertically. Optional \strong{titles}
above each image and configurable spacing between images. This function
\strong{does not save} the workbook; it returns the modified \code{Workbook}.
}
\details{
\itemize{
\item Title rows are written via \code{write_cell()} (bold, left-aligned).
\item Images are inserted via \code{openxlsx::insertImage()}.
\item The next start row in stack mode advances by
\code{ceiling(height * rows_per_inch) + row_spacer} (plus 1 if a title row is present).
}
}
\examples{
\dontrun{
wb <- openxlsx::createWorkbook()
save_image_wb(
  image        = c("First" = "plot1.png", "Second" = "plot2.jpg"),
  wb           = wb,
  sheet        = "MyImages",
  rc           = c(1L, 1L),
  image_titles = c("First Image", "Second Image"),
  width        = 6, height = 4, dpi = 300,
  row_spacer   = 2L
)
openxlsx::saveWorkbook(wb, "images_stacked.xlsx", overwrite = TRUE)
}

}
\seealso{
\code{\link[=save_image_xlsx]{save_image_xlsx()}}, \code{\link[=save_image_wb_split]{save_image_wb_split()}}, \code{\link[openxlsx:insertImage]{openxlsx::insertImage()}}
}
