% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/date.R
\name{as_ymd_safe}
\alias{as_ymd_safe}
\title{Safely coerce to "YYYYMMDD" string (ISO-first)}
\usage{
as_ymd_safe(x)
}
\arguments{
\item{x}{character | factor | Date | POSIXt vector}
}
\value{
A character vector formatted as \code{"YYYYMMDD"}.
}
\description{
Vectorized date-to-string converter that accepts character, factor, Date, or POSIXt.
It mirrors \code{\link[=as_date_safe]{as_date_safe()}} but returns a \code{"YYYYMMDD"} character string
instead of a \code{Date} object.
}
\details{
\strong{Supported formats}
\itemize{
\item ISO-like dates: \code{"YYYY-MM-DD"}, \code{"YYYY/MM/DD"}, \code{"YYYYMMDD"}
\item ISO-like datetimes (time dropped): \code{"YYYY-MM-DD HH:MM:SS"}, \code{"YYYY/MM/DD HH:MM:SS"}
\item Two-digit day/month with "/" or "-":
\code{"DD/MM/YYYY"} vs \code{"MM/DD/YYYY"} (and \code{"DD-MM-YYYY"} vs \code{"MM-DD-YYYY"});
ambiguous cases error.
}

\strong{Rules}
\itemize{
\item \code{Date} inputs are returned formatted.
\item \code{POSIXt} inputs drop the time via \code{as.Date()}.
\item Empty strings \code{""} are treated as \code{NA}.
\item Parsing order: unambiguous ISO(-like) (date or datetime) -> two-digit day/month.
\item Anything not parsed -> error with a short preview.
}
}
\examples{
\donttest{
as_ymd_safe("2024-01-01")           # "20240101"
as_ymd_safe("2024/01/02")           # "20240102"
as_ymd_safe("20240103")             # "20240103"
as_ymd_safe("2024-01-04 12:00:00")  # "20240104"
as_ymd_safe(as.Date("2024-01-05"))  # "20240105"
}

\dontrun{
# Ambiguous day/month cases raise an error
as_ymd_safe("01/08/2017")
}

}
\seealso{
\code{\link[=as_date_safe]{as_date_safe()}}
}
