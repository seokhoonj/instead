% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{set_lower_names}
\alias{set_lower_names}
\alias{set_upper_names}
\title{Convert column names to lowercase (in-place if possible)}
\usage{
set_lower_names(x)

set_upper_names(x)
}
\arguments{
\item{x}{A \code{data.frame}, \code{tibble}, or \code{data.table} whose column names will be
converted to lowercase.}
}
\value{
Invisibly returns the modified object, typically the same object
(in place for \code{data.table} and usually for \code{data.frame} as well).
}
\description{
Converts all column names of a \code{data.frame}, \code{tibble}, or \code{data.table}
to lowercase.
The function uses \code{\link[data.table:setattr]{data.table::setnames()}} internally, which directly modifies
the column names of the input object.
}
\details{
If the input is a \code{data.table}, modification is done \strong{by reference}
(no copy).
For \code{data.frame} and similar objects, the operation usually modifies
the object in place as well (since \code{setnames()} works on name attributes),
but may occasionally trigger a shallow copy depending on how the object
is referenced internally.
}
\examples{
\dontrun{
library(data.table)
dt <- data.table(A = 1, B = 2)
set_lower_names(dt)
names(dt) # "a", "b"

df <- data.frame(A = 1, B = 2)
set_lower_names(df)
names(df) # "a", "b"
}

}
