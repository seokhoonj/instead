% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/excel.R
\name{save_plot_xlsx_split}
\alias{save_plot_xlsx_split}
\title{Save ggplot object(s) to seperate Excel sheets (one plot per sheet)}
\usage{
save_plot_xlsx_split(
  plot,
  file,
  rc = c(1L, 1L),
  sheet_names = NULL,
  width = 8,
  height = 4,
  dpi = 300,
  plot_titles = NULL,
  title_size = 16,
  font_name = getOption("instead.font"),
  overwrite = FALSE
)
}
\arguments{
\item{plot}{A ggplot object or a non-empty list of ggplot objects.}

\item{file}{Path to the target \code{.xlsx} workbook.}

\item{rc}{Common start cell c(row, col) used for all sheets. Default c(1L, 1L).}

\item{sheet_names}{Optional character vector of sheet names; length must equal length(plot).}

\item{width, height}{Plot size in inches for openxlsx::insertPlot().}

\item{dpi}{Image resolution for openxlsx::insertPlot(). Default 300.}

\item{plot_titles}{Optional character vector of titles (one per plot); written one row above the plot.}

\item{title_size}{Numeric font size for titles. Default 16.}

\item{font_name}{Font family for title text. Default getOption("instead.font").}

\item{overwrite}{Logical; overwrite existing workbook on save? Default FALSE.}
}
\value{
(Invisibly) the path to \code{file}. Side-effects: creates/updates an Excel file.
}
\description{
Writes each ggplot to its own worksheet using \code{openxlsx}. A common start cell
(\code{rc}) is applied to all sheets. Sheet names and per-sheet title rows are optional.
}
\details{
Sheet name resolution priority:
\enumerate{
\item \code{sheet_names} argument (if provided),
\item \code{names(plot)} (if present and non-empty),
\item "Sheet 1", "Sheet 2", ...
}
}
\examples{
\dontrun{
library(ggplot2)

p1 <- ggplot(mtcars, aes(mpg, wt)) + geom_point()
p2 <- ggplot(mtcars, aes(hp, qsec)) + geom_point()

save_plot_xlsx_split(
  plot        = list(Fit1 = p1, Fit2 = p2),
  file        = "plots_split.xlsx",
  rc          = c(1L, 1L),
  sheet_names = c("Scatter MPG~WT", "Scatter HP~QSEC"),
  plot_titles = c("MPG vs Weight", "HP vs QSEC"),
  title_size  = 16,
  width = 8, height = 4, dpi = 300,
  overwrite   = TRUE
)
}

}
