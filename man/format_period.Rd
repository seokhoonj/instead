% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/date.R
\name{format_period}
\alias{format_period}
\title{Format Date values as period labels}
\usage{
format_period(
  x,
  type = c("month", "quarter", "half", "year", "day"),
  sep = ".",
  abb = FALSE
)
}
\arguments{
\item{x}{A \code{Date} vector (or an object coercible to \code{Date}).}

\item{type}{Period unit to format. One of \code{"month"}, \code{"quarter"},
\code{"half"}, \code{"year"}, or \code{"day"}.}

\item{sep}{Separator placed between year and period components.
Default is \code{"."}.}

\item{abb}{Logical. If \code{TRUE}, use a 2-digit year (for example, \code{"24.Q1"}).
If \code{FALSE}, use a 4-digit year (for example, \code{"2024.Q1"}).}
}
\value{
A character vector of the same length as \code{x}.
}
\description{
Convert a \code{Date} vector into formatted labels by month, quarter,
half-year, year, or day.
}
\details{
Output formats are:
\itemize{
\item \code{"month"}: \code{"2024.02"} or \code{"24.02"}
\item \code{"quarter"}: \code{"2024.Q1"} or \code{"24.Q1"}
\item \code{"half"}: \code{"2024.H1"} or \code{"24.H1"}
\item \code{"year"}: \code{"2024"} or \code{"24"}
\item \code{"day"}: \code{"2024.02.01"} or \code{"24.02.01"}
}
}
\examples{
\donttest{
d <- as.Date(c("2024-02-01", "2024-07-01"))

format_period(d, "month")
format_period(d, "quarter")
format_period(d, "half")
format_period(d, "year")
format_period(d, "day")

format_period(d, "quarter", abb = TRUE)
format_period(d, "month", sep = "-")
}

}
