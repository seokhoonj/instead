% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/excel.R
\name{save_image_wb_split}
\alias{save_image_wb_split}
\title{Write image file(s) to separate worksheets in an existing Workbook}
\usage{
save_image_wb_split(
  image,
  wb,
  rc = c(1L, 1L),
  sheet_names = NULL,
  image_titles = NULL,
  title_size = 14,
  font_name = getOption("instead.font"),
  width = 12,
  height = 6,
  dpi = 300
)
}
\arguments{
\item{image}{Character vector (or list) of file paths.}

\item{wb}{An existing \code{openxlsx::Workbook}.}

\item{rc}{Length-2 integer vector \code{c(row, col)} used on all sheets.
Default \code{c(1L, 1L)}.}

\item{sheet_names}{Optional character vector of sheet names (length = \code{length(image)}).
If \code{NULL}, falls back to \code{names(image)} or \code{"Sheet {i}"}.}

\item{image_titles}{Optional character vector of titles (length = \code{length(image)}),
written \strong{above} each image. If \code{NULL}, no titles are written.}

\item{title_size}{Numeric font size for titles. Default \code{14}.}

\item{font_name}{Font family for titles. Default \code{getOption("instead.font")}.}

\item{width, height, dpi}{Passed to \code{openxlsx::insertImage()}. Defaults
\code{width = 12}, \code{height = 6}, \code{dpi = 300}.}
}
\value{
The modified \code{Workbook} (invisibly).
}
\description{
Each image file is inserted into its \strong{own worksheet} in an existing
\code{openxlsx} \code{Workbook}. A common start cell \code{rc} is used for all sheets.
Optional per-sheet title rows. This function \strong{does not save} the workbook;
it returns the modified \code{Workbook}.
}
\details{
Sheet names are resolved in this order:
\enumerate{
\item \code{sheet_names} (if provided)
\item \code{names(image)} (when available)
\item \code{"Sheet 1"}, \code{"Sheet 2"}, ...
}
\itemize{
\item Title rows are written via \code{write_cell()} (bold, left-aligned).
\item Images are inserted via \code{openxlsx::insertImage()}.
}
}
\examples{
\dontrun{
wb <- openxlsx::createWorkbook()
save_image_wb_split(
  image        = c("Left plot" = "plot1.png", "Right plot" = "plot2.jpg"),
  wb           = wb,
  rc           = c(2L, 2L),
  sheet_names  = c("Left", "Right"),
  image_titles = c("Left Image", "Right Image"),
  width        = 10, height = 5, dpi = 300
)
openxlsx::saveWorkbook(wb, "images_split.xlsx", overwrite = TRUE)
}

}
\seealso{
\code{\link[=save_image_xlsx_split]{save_image_xlsx_split()}}, \code{\link[=save_image_wb]{save_image_wb()}}, \code{\link[openxlsx:insertImage]{openxlsx::insertImage()}}
}
