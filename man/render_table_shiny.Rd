% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/image.R
\name{render_table_shiny}
\alias{render_table_shiny}
\title{Render an HTML table for Shiny output}
\usage{
render_table_shiny(table, ...)
}
\arguments{
\item{table}{A data.frame-like object to be passed to \code{render_table_html()}.}

\item{...}{Additional arguments passed directly to \code{render_table_html()}
(e.g., caption, digits, full_width, html_font).}
}
\value{
A \code{tagList} containing \verb{<style>} and HTML \verb{<table>} markup suitable
for returning from a Shiny \code{renderUI()} expression.
}
\description{
Wrap an HTML table (created by \code{render_table_html()}) with additional CSS
styling and return a \code{tagList} suitable for use inside \code{renderUI()} or
\code{uiOutput()} in Shiny applications.
}
\details{
This function converts the kable object into a character string and embeds it
inside an HTML block so that Shiny can display it as intended.
}
\examples{
\dontrun{
# Minimal working Shiny example
app <- shiny::shinyApp(
  ui = shiny::fluidPage(
    shiny::h3("Example Table"),
    shiny::uiOutput("tbl")
  ),

  server = function(input, output, session) {
    output$tbl <- shiny::renderUI({
      render_table_shiny(
        head(mtcars),
        caption = "MTCARS Example"
      )
    })
  }
)

shiny::runApp(app)
}

}
